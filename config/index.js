const config = {
    "apiPrefix": "http://localhost:8081",
    "serverPort": "8081",
    "db": {
        "name": "blog",
        "host": "localhost",
        "port": 27017,
        "secret": "verysecretkey"
    }
};

export default config;